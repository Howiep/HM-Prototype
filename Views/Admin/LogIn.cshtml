@model HeedeMoestrup.Models.ViewModels.LogInModel
@{
    ViewBag.Title = "LogIn";
    Layout = "~/Views/Shared/_AdminLayoutPage.cshtml";
}

<h2>Log In</h2>


@Html.ValidationSummary(true)

@using (Html.BeginForm())
{
    @Html.EditorForModel()
    <p>
        <button type="submit">Log In</button>
    </p>
}